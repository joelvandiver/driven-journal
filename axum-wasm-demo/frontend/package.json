{
  "name": "axum-wasm-demo-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "React + TypeScript frontend for the axum-wasm-demo: calls the same Axum API served natively (MUSL) or via WASI/WASM (wasmtime serve).",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.8",
    "@types/react-dom": "^19.0.3",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
